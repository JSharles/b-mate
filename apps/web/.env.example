# Copy to .env.local for local development: cp .env.example .env.local
# In production (Vercel/Railway), these are set via the platform dashboard, not a committed file.

NEXT_PUBLIC_API_URL="http://localhost:3001"
