node_modules/
.pnpm-store/
dist/
build/
coverage/
.next/
.next-*/
out/
.turbo/
.impeccable/
*.log
.env
.env.*
!.env.example
.DS_Store
Thumbs.db
*.tmp
*.swp
.vscode/
.idea/

# Claude Code local settings — contains this machine's approved permissions,
# which can include secrets typed into allowed command patterns (e.g. an API
# key passed inline to a CLI). Never commit.
.claude/settings.local.json

# Codex tooling — this repo standardises on .claude/ (the Impeccable skill
# under .agents/ is a duplicate of the tracked one). Not used any more.
.agents/
.codex/
